plugins {
    id 'java'
}

group 'com.test.idm'
version '1.0'

sourceCompatibility = 1.8

test {
    testLogging {
        events "failed"
        exceptionFormat "short"
    }
}

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
